import 'package:flutter/material.dart';

class CoustomOtherTwo extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      height: 40,
      child: const VerticalDivider(
        color: Colors.grey,
      ),
    );
  }
}
